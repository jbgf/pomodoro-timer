import { JSX as JSX_2 } from 'react';
import { PomodoroSettings } from '../../types';

declare function PomodoroTimer({ settings }: Props): JSX_2.Element;
export default PomodoroTimer;

declare interface Props {
    settings: PomodoroSettings;
}

export { }
